export const metadata = {
    title: 'Profile',
    description: 'Next Hero',
}
const ProfilePage = () => {
    return (
        <div>
            profile page
        </div>
    );
};

export default ProfilePage;